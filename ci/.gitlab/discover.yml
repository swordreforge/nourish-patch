# The link-drift guard. The former per-workspace fan-out (generate-child + per-entry,
# GitLab's equivalent of the GitHub matrix) was removed: CI builds only the root product
# (release / pages), not every workspace independently.

link-drift:
  stage: discover
  script:
    - ci/scripts/link-drift.sh
