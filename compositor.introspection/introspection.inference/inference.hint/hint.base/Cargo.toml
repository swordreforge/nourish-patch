[package]
name = "compositor_introspection_inference_hint_base"
version = "0.1.0"
edition = "2021"

[dependencies]
compositor_introspection_extraction_window_base.workspace = true
compositor_introspection_inference_hint_data = { workspace = true }
compositor_introspection_inference_hint_descriptors = { workspace = true }

[lib]
path = "lib.rs"
