//! Native Vulkan parallax background pipelines (SDR + HDR variants), extracted
//! from `renderer.core` so they live beside the other composite pipelines under
//! `vulkan.pipeline`.

pub mod background;
