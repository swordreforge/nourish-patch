[package]
name = "compositor_remote_message_macro_base"
version = "0.1.0"
edition = "2024"

[lib]
path = "lib.rs"

[dependencies]
compositor_remote_message_macro_define = { workspace = true }
compositor_remote_message_macro_broadcast = { workspace = true }
