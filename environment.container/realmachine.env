# Debugging
EGL_LOG_LEVEL=debug

# Backend Forcing (Only if things don't auto-detect)
WINIT_UNIX_BACKEND=wayland
GDK_BACKEND=wayland
CLUTTER_BACKEND=wayland

# Intel-specific VA-API acceleration (for video)
LIBVA_DRIVER_NAME=iHD