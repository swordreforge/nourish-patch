pub mod protocol;

pub mod advertise_factory_fuunction {
    // Was on compositor::new to advertise the supported custom wayland protocol
    // // Advertise monitor protocol
    // loader
    // .display_handle
    // .create_global::<Loop, Y5CompositorManagerV1, ()>(1, ());
}